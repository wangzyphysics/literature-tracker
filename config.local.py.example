"""
本地配置文件示例（不会被提交到 git）
复制为 config.local.py 并填入你自己的密钥：

  cp config.local.py.example config.local.py
"""

# AI 配置（推荐：OpenRouter）
AI_CONFIG = {
    "provider": "openrouter",
    "api_key": "sk-or-REPLACE_ME",
    # OpenRouter 模型（provider=openrouter 时生效）
    "model": "stepfun/step-3.5-flash:free",
}

# 邮件配置（可选）
EMAIL_CONFIG = {
    "sender_email": "",
    "sender_password": "",
}

# 微信推送配置（可选）
WECHAT_CONFIG = {
    "enabled": False,
    "sendkey": "",
}

